# Database connection string for local development (PostgreSQL in Docker)
DATABASE_URL="postgresql://coolproject:coolproject@localhost:6767/coolproject"

# Auth.js secret - generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-here"
